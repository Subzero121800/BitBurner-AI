# Screenshots

Drop the three referenced PNGs here as:

| Filename                  | What it shows                                                                |
|---------------------------|------------------------------------------------------------------------------|
| `scb-orchestrator.png`    | scb.js spawning the Ollama Player + the player executing actions             |
| `watchdog-reconnect.png`  | Mass backdoor sweep + the watchdog handling Remote API offline/online cycles |
| `ai-safety-rails.png`     | The cash-reserve guard rejecting an `upgrade_server` action cleanly          |

These are referenced from the top-level [README](../../README.md). Keep
filenames stable so the embeds resolve once you push to GitHub.
