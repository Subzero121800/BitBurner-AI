AI-proposed patches to PROTECTED files land here as
pending-patch.json. Review with:
  run /approve-patch.js                  -- preview
  run /approve-patch.js --approve        -- apply (snapshot saved)
  run /approve-patch.js --discard        -- reject
  run /approve-patch.js --list           -- show history
